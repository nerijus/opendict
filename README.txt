Requirements for OpenDict
-------------------------

   - python >=2.2
   - wxPython 2.4 for python >=2.2
   - python-xml for python >=2.2

Quick Installation
------------------

   OpenDict on UNIX systems can be installed in a quick & dirty way:

      # make install
   
   Get .deb file if you use Debian and install it with dpkg program.
